#include <stdio.h>
#include <limits.h>
int main(void)
{
    int n,s,m,l;
    int scan = scanf("%d%d%d%d",&n,&s,&m,&l);

    int min = INT_MAX;
    for(int i=0;;i++) {
        for(int j=0;;j++) {
            for(int k=0;;k++) {
                

            }
        }
    }
}