#include <stdio.h>
int main(void)
{
    int n,l,r;
    int scan = scanf("%d%d%d",&n,&l,&r);
    for(int i=0;i<n;i++) {
        int tmp;
        scan = scanf("%d",&tmp);
        int cnt = 0;
        for(int j=l;j<=r;j++) {
            
        }
    }
}